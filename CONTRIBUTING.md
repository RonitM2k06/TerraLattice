# Contributing to TerraLattice

We welcome contributions from researchers, scientists, and engineers!

## Development Setup
1. Clone the repository
2. `pip install -e .`
3. `cd frontend && npm install && npm run dev`

## Scientific Integrity
Any changes to the causal graph parameters (`parameters/*.yaml`) must be accompanied by citations in the PR description and a run of the validation test suite.
